resources:
  schemas:
    bronze:
      name: bronze
      catalog_name: ${var.catalog}
      comment: 'Organizational schema including bronze tables and objects in the medallion architecture of the lakehouse.'
      lifecycle:
        prevent_destroy: true